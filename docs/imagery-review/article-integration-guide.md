# Article imagery integration — 6 September 2026

Ready for integration: 44 individually generated and visually inspected article assets, supplementing the four approved company photographs. The 109 location guides remain text guides. No shared manifest, page source, build, deployment or authentic photograph was changed by this preparation task.

Merge the `assets` object from `article-intake-additions.json` into the approved unique asset intake, the `owners` object from `article-owner-additions.json` into the owner registry, and all entries from `article-responsive-additions.json` into the responsive registry. Each asset belongs to exactly one article. Reuse for that article's thumbnail, lead, sharing image and structured data is intentional; assigning it to another article is not.

The 44 masters are 1536 × 1024 WebP. Each family has 320, 640, 960 and 1536 pixel candidates, for 176 files totalling 12,333,386 bytes. Files live in `assets/photos/files/Editorial-2026/`. Preserve the full 3:2 composition in the article lead; test directory crops independently, especially circular drum flanges, the boat bulkhead, the door slab and the paired wall comparisons.

Every generated output was viewed individually. Four final contact sheets were reviewed as a set for composition diversity and brand consistency. The palette uses natural timber, grey workshop surfaces and restrained green accents. Subjects address the source article: material edges, joinery applications, packing components, comparisons, inspection concepts and loading models. None is documentary evidence of a Cochin Wood facility, a real shipment, an identified timber species, a tested rating or a certificate. Captions describe visible materials and practical considerations; no customer-facing generation labels were added. Source PNG creation credentials are retained and every WebP carries IPTC DigitalSourceType trainedAlgorithmicMedia.

Three corrections are complete, and no rejected master is assigned:

- HT stamp validity: removed the stamp-like patch, retaining the repaired timber corner and inspection tools.
- Calibrated furniture plywood: replaced the incorrectly contacting micrometer with a tool resting unused beside the panels.
- Core comparison: replaced the inconsistent upright blocks with three flat plywood offcuts showing straight layered edges. Colour differences are not a species identification claim.

The exact original and correction prompts, copied original hashes, rejected histories and accepted asset hashes are in the intake and progress files. Rejected originals are retained under `article-originals/` for audit only. This review archive must not be added to public image placements.

The preparation script verified every WebP decodes, matches its declared width and contains the generated-source metadata; it also verified unique accepted source and master mappings. Root integration still needs the independent 48-article check of lead/card/share/schema ownership and browser rendering, alongside existing preservation and responsive-image gates. No publication has been performed by this subtask.
